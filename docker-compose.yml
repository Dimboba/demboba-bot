services:
  db:
    image: 'postgres:13.1-alpine'
    container_name: database
    environment:
      POSTGRES_DB: "dembobabot"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "qwe"
      POSTGRES_ROOT_PASSWORD: "qwe"
    ports:
      - "5432:5432"